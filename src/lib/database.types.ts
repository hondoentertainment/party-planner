/**
 * Database types for the app. The schema snapshot lives in
 * `database.types.gen.ts`; regenerate with `npm run db:types` (requires
 * `SUPABASE_PROJECT_ID` and `npx supabase login`) and merge/diff before commit.
 */
export * from "./database.types.gen";
